:javascript
  google.load("visualization", "1", {packages:["corechart"]});
  google.setOnLoadCallback(drawChart);

  function drawChart() {



       response = $.ajax({
                               url: "#{"/consumer/restaurants/#{@restaurant.id}/restaurant_features"} ",
                               dataType:"json",
                               async: false
                               }).responseText;
     jsonData = $.parseJSON(response);
     columns = jsonData['columns'];
     rows = jsonData['rows'];

      var data = new google.visualization.DataTable();
      data.addColumn('date', 'Date');

      for (var i = 0; i < columns.length; i++) {
         data.addColumn('number',columns[i] ,{role: 'annotation'}  );
      }
      console.log("drwaing again yo");
      for(var i=0; i< rows.length; i++){
        rows[i][0] = new Date(rows[i][0]*1000);
      }
      data.addRows(rows);

      var options = {
          pointSize: 5,
          vAxis: {format:'#',
                    viewWindow:{
                      max:5,
                      min:0
                    }
                  }
      };

      var chart = new google.visualization.LineChart(document.getElementById('chart_div'));
      chart.draw(data, options);
  }
.row-fluid
  .span3.sidebar
    = render :partial => 'sidebar'

  .span9.analytics
    = render :partial => 'subheading', :locals => {:heading => 'Restaurant'}

    #reviews_score
    Restaurant
    = @reviews_score.category_score "restaurant"

    - @reviews_score.category_features_scores("restaurant").each do |k,v|
      = k
      = v
    


    %h4 Restaurant Trend
    #chart_div